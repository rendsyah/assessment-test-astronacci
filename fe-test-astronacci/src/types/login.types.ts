export type LoginForm = {
  email: string;
  password: string;
};

export type LoginOAuthForm = {
  token: string;
};

export type RegisterForm = {
  name: string;
  email: string;
  password: string;
  membership: string;
};
